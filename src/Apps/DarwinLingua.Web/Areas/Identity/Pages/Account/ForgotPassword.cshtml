@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Forgot password";
}

<section class="panel stack-lg">
    <span class="eyebrow">Account recovery</span>
    <h1 class="headline-md">Forgot your password?</h1>
    <p class="body-lg">Enter your email address and Darwin Lingua will send a recovery link if the account can be reset.</p>
</section>

<section class="panel stack-md">
    <form method="post" class="form-grid">
        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
        <label asp-for="Input.Email"></label>
        <input asp-for="Input.Email" autocomplete="email" />
        <span asp-validation-for="Input.Email" class="text-danger"></span>
        <div class="action-row">
            <button type="submit" class="button button--primary">Send reset link</button>
            <a class="button button--secondary" asp-area="Identity" asp-page="/Account/Login">Back to sign in</a>
        </div>
    </form>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
